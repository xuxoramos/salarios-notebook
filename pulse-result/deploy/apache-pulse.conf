# Added inside the thedata.pub *:443 vhost (ProxyPreserveHost is already On there).
# Port 8010 is taken by the dssg-calibration proxy; sg-pulse runs on 8020.
ProxyPass        /pulse http://127.0.0.1:8020/pulse retry=0
ProxyPassReverse /pulse http://127.0.0.1:8020/pulse
