- The options in questions `primary_role` and `secondary_role` don't capture middle management and leadership positions nor technical roles transitioninig into management (i.e. primary role = Backend Dev, secondary_role = Project Manager), and they are also respondents of the survey. Discuss merging technical roles and expanding options for middle management and leadership, adding the data analyst job, and the disambiguation of PM (program or project or product manager).
- We want to capture beneath `secondary_role` the possibility of resondents having a 2nd job. It can by anything. We just want to capture the existance of it, not go into any details, since in Latin America, having 2 jobs is frowned upon, and even penalized by the employer who pays the larger chunk of your salary.
tienes otro trabajo (justo abajo)? cual?
- Questions `primary_language`, `primary_framework`, `primary_database`, `primary_cloud`, `primary_lang_years`, `tech_breadth`, `stack_change` should only appear if the answer to `primary_role` is technical. It is not necessary to filter out available options for each available answer to `primary_role`.
- How can we capture the prevalence of vibe coding and how this plays into the drift of `primary_language` or `primary_framework` from the professional identity of respondents?
- `tech_breadth` is numerical. discuss its relevance against `secondary_role`, and if still relevant, discuss converting it into a multiple selection answer.
- Answers to `cert_category` should only include the most common certifications in the market and add OTHER. Also remove `cert_count`.
- Is `payment_currency` too down below the survey? should it be part of the compensation section (the one that includes `base_salary`, `total_cash_annual`, etc)? 
- Delete question `cross_border_tax`
- Answers to question `financial_savings` should be sensible percentage ranges. Add also a question that captures if the responent also actively invests and in what instruments (government bonds, ETFs, funds or even stock market)
- Help us discuss how to capture social mobility. Many technologists in Latin America are the first in their families to have a university education, and even go into it knowingly because it gives them social mobility.
- Phrasing of `has_equity` should explicitly refer to equity from the current employer.
- add question `women_mentorship_institutional` that captures if the employer has specific, insitutional programs that promote or mentor women for leadership positions. Add it to the appropriate section.
- how can we capture if the employer offers longer paternity leave than what the respondent's law requires?
- We are realizing also that even though we have sections that capture experience of working conditions of male, female and non-binary employees, we are not asking how a respondent identifiy themselves. we should ask that question explicitly in the beginning.
